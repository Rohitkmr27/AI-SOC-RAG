function App() {
  return (
    <main className="page-shell">
      <section className="status-card" aria-labelledby="page-title">
        <p className="eyebrow">Stage 1</p>
        <h1 id="page-title">AI-SOC-RAG is running</h1>
        <p>
          The React frontend is ready. Security operations features will be added
          in later stages.
        </p>
      </section>
    </main>
  );
}

export default App;
